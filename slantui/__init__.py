"""SlantUI: a window look, as a library.

An oblique title bar drawn by the page, a frameless Qt window that still
animates and snaps like a native one, a widget set with no hairline dividers
anywhere, and a two level token system that keeps all of it readable on eight
palettes.

Using SlantUI means keeping the credit line it draws in the title bar. See
LICENSE.
"""
from __future__ import annotations

__version__ = "0.2.0"
__all__ = ["__version__"]
